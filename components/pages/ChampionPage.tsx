import styled from "@emotion/styled";
import React from "react";

const Base = styled.main``;

const ChampionPage: React.FC = () => {
  return <Base>ChampionPage</Base>;
};

export default ChampionPage;
